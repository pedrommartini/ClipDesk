<UserControl x:Class="ClipDesk.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ClipDesk.Views"
             Foreground="{DynamicResource TextBrush}" Background="{DynamicResource SurfaceBrush}">
    <UserControl.Resources>
        <local:HistoryThumbnailConverter x:Key="ThumbnailConverter" />
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" CornerRadius="8" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource HoverBrush}" /></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource HoverBrush}" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryWorkspaceButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource AccentStrongBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Surface" Property="Opacity" Value="0.88" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Surface" Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98" /></Setter.Value></Setter></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="CLIPDESK / RECENTES" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,6" />
                <TextBlock Text="Histórico" FontSize="24" FontWeight="SemiBold" />
            </StackPanel>
            <Button Grid.Column="1" Width="34" Height="34" VerticalAlignment="Top" Click="Close_Click" ToolTip="Fechar histórico (Esc)" AutomationProperties.Name="Fechar histórico">
                <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12" />
            </Button>
        </Grid>
        <Border Grid.Row="1" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="9" Padding="10,7">
            <DockPanel>
                <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center" Margin="0,0,9,0" />
                <Grid>
                    <TextBox x:Name="SearchBox" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextBrush}" CaretBrush="{DynamicResource TextBrush}" FontSize="13" TextChanged="Search_TextChanged" AutomationProperties.Name="Buscar no histórico" />
                    <TextBlock Text="Buscar no histórico" Foreground="{DynamicResource MutedBrush}" IsHitTestVisible="False">
                        <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value=""><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                    </TextBlock>
                </Grid>
            </DockPanel>
        </Border>
        <Grid Grid.Row="2" Margin="0,14,0,10">
            <TextBlock x:Name="CountText" FontSize="11" Foreground="{DynamicResource MutedBrush}" />
            <TextBlock Text="MAIS RECENTES PRIMEIRO" FontSize="9" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Right" />
        </Grid>
        <Grid Grid.Row="3">
            <ListBox x:Name="HistoryList" Background="Transparent" BorderThickness="0" Padding="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                     PreviewMouseLeftButtonDown="List_MouseDown" MouseMove="List_MouseMove" MouseDoubleClick="List_DoubleClick" PreviewKeyDown="List_KeyDown"
                     AutomationProperties.Name="Itens recentes do clipboard">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,8" />
                        <Setter Property="Template"><Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Card" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True"><Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /></Trigger>
                                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Card" Property="Background" Value="{DynamicResource HoverBrush}" /></Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value></Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                            <Grid>
                                <TextBlock Text="{Binding TypeLabel}" FontSize="10" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding CapturedAt, StringFormat='{}{0:dd/MM · HH:mm}'}" FontSize="10" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Right" />
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,9,0,8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <Image Source="{Binding StoredFilePath, Converter={StaticResource ThumbnailConverter}}" Width="48" Height="48" Stretch="Uniform" Margin="0,0,10,0">
                                    <Image.Style><Style TargetType="Image"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><Trigger Property="Source" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed" /></Trigger></Style.Triggers></Style></Image.Style>
                                </Image>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextBrush}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding Preview}" Foreground="{DynamicResource MutedBrush}" FontSize="12" TextWrapping="Wrap" MaxHeight="34" Margin="0,4,0,0" />
                                </StackPanel>
                            </Grid>
                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="＋ Mesa" Click="Add_Click" Foreground="{DynamicResource AccentBrush}" ToolTip="Adicionar à mesa" AutomationProperties.Name="Adicionar à mesa" Margin="0,0,4,0" />
                                <Button Content="Copiar" Click="Copy_Click" Background="{DynamicResource SurfaceBrush}" AutomationProperties.Name="Copiar item" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel x:Name="EmptyState" VerticalAlignment="Center" Margin="12" IsHitTestVisible="False">
                <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="32" HorizontalAlignment="Center" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,14" />
                <TextBlock x:Name="EmptyTitle" Text="Seu próximo item começa com Ctrl+C" TextAlignment="Center" FontWeight="SemiBold" TextWrapping="Wrap" Foreground="{DynamicResource TextBrush}" />
                <TextBlock x:Name="EmptyHint" Text="Copie um texto, link, imagem ou arquivo. Ele aparecerá aqui enquanto o ClipDesk estiver aberto." TextAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}" FontSize="12" Margin="0,8,0,0" />
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="4" Margin="0,10,0,0">
            <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,0,0,10" />
            <TextBlock x:Name="StatusText" Text="Arraste para a mesa · Duplo clique para copiar" TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource MutedBrush}" />
            <Button x:Name="OpenWorkspaceButton" Content="Abrir mesa de trabalho  ↗" Click="OpenWorkspace_Click" Visibility="Collapsed" HorizontalAlignment="Stretch" Style="{StaticResource PrimaryWorkspaceButton}" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
