name: Publicar landing page

on:
  push:
    branches: [main]
    paths:
      - "landing/**"
      - "docs/ClipDesk-Plugin-AI-DevKit.zip"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Obter projeto
        uses: actions/checkout@v4
      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v5
      - name: Preparar landing page
        run: |
          mkdir -p landing/downloads
          cp docs/ClipDesk-Plugin-AI-DevKit.zip landing/downloads/ClipDesk-Plugin-AI-DevKit.zip
      - name: Enviar landing page
        uses: actions/upload-pages-artifact@v3
        with:
          path: landing
      - name: Publicar
        id: deployment
        uses: actions/deploy-pages@v4
